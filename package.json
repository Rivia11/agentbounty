{
  "name": "agent-bounty",
  "version": "1.0.0",
  "description": "Autonomous AI agent that accepts bounties via x402 payments",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "agent:dev": "pnpm --filter agent dev",
    "agent:build": "pnpm --filter agent build",
    "agent:start": "pnpm --filter agent start",
    "web:dev": "pnpm --filter web dev",
    "web:build": "pnpm --filter web build",
    "web:start": "pnpm --filter web start",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "keywords": ["ai-agent", "x402", "crypto", "bounty", "autonomous"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
